<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

  <!-- Borderless window chrome.
       CaptionHeight is non-zero on purpose: it lets Windows own the caption, so dragging,
       double-click maximize, right-click system menu, Aero Snap and the Win11 snap layouts all
       keep working. Interactive elements in that strip opt out with
       WindowChrome.IsHitTestVisibleInChrome="True" (see Button.Caption). -->
  <Style x:Key="Window.Shell" TargetType="Window">
    <Setter Property="Background" Value="{StaticResource Brush.Background}" />
    <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
    <Setter Property="FontFamily" Value="{StaticResource Font.Ui}" />
    <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}" />
    <Setter Property="WindowStyle" Value="None" />
    <Setter Property="ResizeMode" Value="CanResize" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="UseLayoutRounding" Value="True" />
    <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
    <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    <Setter Property="shell:WindowChrome.WindowChrome">
      <Setter.Value>
        <shell:WindowChrome CaptionHeight="{StaticResource Size.CaptionHeight}"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            NonClientFrameEdges="None"
                            UseAeroCaptionButtons="False" />
      </Setter.Value>
    </Setter>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Window">
          <Border x:Name="PART_Frame"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{StaticResource Brush.BorderStrong}"
                  BorderThickness="1"
                  SnapsToDevicePixels="True">
            <!-- AdornerDecorator has to be reinstated by hand in a custom Window template,
                 otherwise tooltips, popups and validation adorners have nowhere to render. -->
            <AdornerDecorator>
              <ContentPresenter />
            </AdornerDecorator>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="WindowState" Value="Maximized">
              <!-- The frame line would sit under the screen edge and read as a stray pixel row. -->
              <Setter TargetName="PART_Frame" Property="BorderThickness" Value="0" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Modal dialogs: same chrome, tighter caption, no maximize. -->
  <Style x:Key="Window.Dialog" TargetType="Window" BasedOn="{StaticResource Window.Shell}">
    <Setter Property="Background" Value="{StaticResource Brush.Surface}" />
    <Setter Property="ResizeMode" Value="NoResize" />
    <Setter Property="ShowInTaskbar" Value="False" />
    <Setter Property="WindowStartupLocation" Value="CenterOwner" />
    <Setter Property="shell:WindowChrome.WindowChrome">
      <Setter.Value>
        <shell:WindowChrome CaptionHeight="40"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            NonClientFrameEdges="None"
                            UseAeroCaptionButtons="False" />
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
